---
- name: Run inline PowerShell script
  hosts: all
  gather_facts: false
  tasks:
    - name: Execute multi-lined PowerShell commands
      ansible.windows.win_powershell:
        script: |
          $jobName = "iLand*"
          if ($job) {
            Disable-VBRJob -Job $jobName -Confirm:$false}
            Write-Host "Veeam job '$jobName' has been disabled."
          else
            Write-Host "Veeam job '$jobName' not found."
      register: veeam_disable_output

    - name: Display the result
      ansible.builtin.debug:
